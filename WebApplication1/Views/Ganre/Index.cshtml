@model IEnumerable<WebApplication1.Models.GanreViewModel>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "EditOrCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditOrCreate", new { id=item.Id }) |
            @Html.ActionLink("Delete", "EditOrCreate", new { id=item.Id })
        </td>
    </tr>
}

</table>
