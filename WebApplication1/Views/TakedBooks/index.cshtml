@model IEnumerable<WebApplication1.Models.TakedBooksViewModel>

@{
    ViewBag.Title = "index";
}

<h2>index</h2>

@{Html.RenderPartial("UsersTakedBook");}

<p>
    @Html.ActionLink("Create New", "EditOrCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Books_Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Books_Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditOrCreate", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
